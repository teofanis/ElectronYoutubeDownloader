import '@testing-library/jest-dom';
import { render } from '@testing-library/react';
import { {{pascalCase name}} } from 'components';

describe('{{pascalCase name}} Test', () => {
  it('should render', () => {
    expect(render(<{{pascalCase name}}>Test</{{pascalCase name}}>)).toBeTruthy();
  });
});
